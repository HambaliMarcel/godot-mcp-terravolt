#!/bin/sh
# Strip Cursor-inserted Co-authored-by trailers (local clones only).
# Enable: git config core.hooksPath .githooks
tmp="$(mktemp)"
tr -d '\015' < "$1" | grep -vE '^Co-authored-by:.*cursoragent@cursor\.com' >"$tmp" || true
mv -f "$tmp" "$1"
