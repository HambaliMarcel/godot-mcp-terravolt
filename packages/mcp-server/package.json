{
  "name": "@terravolt/godot-mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Terravolt Node MCP router (stdio ↔ Godot daemon @ :6505). Scaffolding only until task 05.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "test": "node --test \"tests/smoke.test.mjs\""
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.7",
    "eslint": "^9.17.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.19.1"
  }
}
