# Godot MCP addon (placeholder)

Place GDScript/editor plugin sources here.

**Convention:** symlink or copy into a Godot 4 project under `addons/<addon_folder>/` (folder name finalized with the addon’s `plugin.cfg`).
