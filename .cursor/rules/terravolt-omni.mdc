---
description: TerraVolt Omni — Graphify, GitNexus, and repo boundaries
globs: "**/*"
alwaysApply: true
---

# TerraVolt Omni (this workspace)

- Read `docs/architecture/overview.md` before broad refactors.
- **JS module graphs (Omni)**: refresh with `npm run intel:graphs` → `artifacts/js-graphs/` when Node/TS layout under `packages/` or `tools/` changes.
- **Knowledge graph ([safishamsi/graphify](https://github.com/safishamsi/graphify))**: `graphify-out/` (`npm run intel:graphify`); Cursor `graphify.mdc` guides query-first use.
- **GitNexus**: use MCP tools; if stale, run `npm run intel:gitnexus` from repo root.
- **Vendor boundary**: `references/` is upstream study material only—not product source.
- Load context using `docs/context/context-map.md` ordering.
