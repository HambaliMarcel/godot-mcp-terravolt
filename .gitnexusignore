# --- GitNexus: follow .gitignore for normal paths except we set GITNEXUS_NO_GITIGNORE=1
# so clones under references/ ARE analyzed. Exclude noise here instead. ------------

# Sphinx manual clone (massive prose tree — use online docs / local grep instead)
references/godot-docs/

node_modules/

# Local IDE caches (not product code)
.claude/

# Generated / vendor dumps
artifacts/
graphify-out/
dist/
coverage/
