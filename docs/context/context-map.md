# Context map — TerraVolt Godot MCP

Suggested order when loading context:

1. **Structure** — `docs/architecture/overview.md`, `artifacts/js-graphs/*.json`, GitNexus resources (`gitnexus://repo/{name}/context`).
2. **Product intent** — root `README.md`, future `docs/specs/` and `packages/*/README.md`.
3. **References only** — `references/` (upstream clones; never treat as shipped product).

Prefer **paths** and short excerpts over pasting entire generated JSON.
