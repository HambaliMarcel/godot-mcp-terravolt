# Placeholder — enable smoke / integration suites in docs/tasklist/10-quality-testing-release-and-docs.md
name: Test (reserved)

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Reserve path for task 10 (router + addon + headless)."
