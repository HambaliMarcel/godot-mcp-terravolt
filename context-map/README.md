# Context map — TerraVolt Godot MCP

Priority for agents loading context:

1. **Structure** — `architecture/SYSTEM_OVERVIEW.md`, `graphs/*.json`, GitNexus resources (`gitnexus://repo/{name}/context`).
2. **Product intent** — root `README.md`, future `specs/`.
3. **References only** — `references/` (upstream patterns; not shipped product).

Prefer **paths** and short excerpts over pasting entire generated JSON.
