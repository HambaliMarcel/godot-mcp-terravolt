# JS module graphs (Omni toolchain)

Generated by `npm run intel:graphs` (dependency-cruiser + madge).

| File | Source |
|------|--------|
| **dependency-graph.json** | dependency-cruiser |
| **madge-graph.json** | madge (circularity / module summary) |

Reference clones under `references/` and generated trees (`artifacts/`, `graphify-out/`) are excluded from scans.

Refresh `docs/architecture/overview.md` when the MCP server or addon layout changes materially.
